# =============================================================================
# MongoDB Atlas -> DocumentDB Compatibility Scanner
# =============================================================================
# Copy this file to .env and fill in your values.
# The .env file is .gitignore'd and never committed.

# ---------------------------------------------------------------------------
# Required: Atlas Programmatic API Key (project-scoped)
# ---------------------------------------------------------------------------
# Create at: Atlas > Project Settings > Access Manager > API Keys
# Required permissions: Project Read Only (minimum)
atlas_public_key=YOUR_PUBLIC_KEY
atlas_private_key=YOUR_PRIVATE_KEY

# Atlas project (group) ID — find it in the Atlas URL or Project Settings
atlas_group_id=YOUR_GROUP_ID

# ---------------------------------------------------------------------------
# Optional: Scan a single cluster (omit to scan all clusters in the project)
# ---------------------------------------------------------------------------
# atlas_cluster_name=my-cluster

# ---------------------------------------------------------------------------
# Optional: Organization Service Account (enables multi-project scanning)
# ---------------------------------------------------------------------------
# Create at: Atlas > Organization Settings > Access Manager > Service Accounts
# Required permissions: Organization Project Creator, Organization Read Only
# atlas_organization_Client_ID=YOUR_SA_CLIENT_ID
# atlas_organization_Client_Secret=YOUR_SA_CLIENT_SECRET

# ---------------------------------------------------------------------------
# Optional: Tuning
# ---------------------------------------------------------------------------
# Number of days of Atlas logs to download and scan (default: 7)
# ATLAS_LOG_DAYS=7

# Hourly rate for cost estimates in the report (default: 0 = omit cost column)
# ENGINEER_HOURLY_RATE=150
